# Amateras Router
Router 是实现路由的标准模块，使用此模块就能轻松规划页面路径。

## 优势
- 路径参数类型保护
- 页面路径别名机制
- 路径分组功能
- 组件化页面

## 使用方式
使用 `$.router` 函数定义路由构造器（Router Constructor）。
```ts
import 'amateras';
import 'amateras/router';
```

## 文档
[路由器](/docs/Router.md)